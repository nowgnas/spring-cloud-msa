spring:
    datasource:
        driver: com.h2.Driver
        url: dbUrl
        username: admin
        password: admin

jwt:
  token:
    key: my_token_secret_key

default:
    message: user-service 에서 global profiles 에서 사용할 설정 정보들
