spring:
  cloud:
    config:
      uri: http://host.docker.internal:8888
      name: user-service
      profile: dev
management:
  endpoints:
    web:
      exposure:
        include: refresh, busrefresh