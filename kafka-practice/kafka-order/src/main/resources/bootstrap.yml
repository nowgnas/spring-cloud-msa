spring:
  cloud:
    config:
      uri: http://host.docker.internal:8888
      name: order-service
      profile: dev
management:
  endpoints:
    web:
      exposure:
        include: refresh, busrefresh