spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: order-service
      profile: dev
management:
  endpoints:
    web:
      exposure:
        include: refresh, busrefresh, health, info